# CLAUDE.md

Follow [AGENTS.md](AGENTS.md) for repository guidance.

## Claude Code plugin

Claude plugin files live in `.claude-plugin/`, `agents/`, and `commands/`.
Verify a local Claude marketplace install with `/plugin`.
